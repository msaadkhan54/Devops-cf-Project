apiVersion: v1
kind: Service
metadata:
  name: saad-service
spec:
  type: NodePort
  selector:
    app: saad-deploy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080