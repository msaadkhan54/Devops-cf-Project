apiVersion: v1
kind: Service
metadata:
  name: shoppingassistantservice
spec:
  selector:
    app: shoppingassistantservice
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
